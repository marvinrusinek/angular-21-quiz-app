// Sort order for the QuizSelection difficulty control.
// 'default' = original quiz.json order; 'asc' = easiest→hardest; 'desc' = hardest→easiest.
export type QuizSortDirection = 'default' | 'asc' | 'desc';
